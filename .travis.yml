language: generic
os:
- osx
before_install: 
script:
- bash ./make_osx.sh
deploy:
  provider: releases
  skip_cleanup: true
  api_key: $GITHUB_TOKEN
  file_glob: true
  file:
  - "pktwallet-*"
  on:
    branch: master
    tags: true